<!DOCTYPE html>
<html>
  <head>
    <title>Basic HTML Page</title>
    <link rel="stylesheet" href="/home.css">
  </head>
  <body>
  <h1><%= @team_name %><h1>
    <% positions = TeamData::ROLL_CALL[:"#{@team_name}"].keys
      positions.each do |position| %>
        <li>
          <%= "Name: " + TeamData::ROLL_CALL[:"#{@team_name}"][:"#{position}"] + ", Position: #{position}"
        %></li>
        <% end %>
    <a href="/teams">Back to Teams</a>
    <br>
    <a href="/">Home</a>
  <script src="/javascripts/app.js"></script>
</body>
</html>
